import { H1 } from "../components/ui/typography";

export const DashboardPage = () => {
	// TODO: Get session (¿userProvider o nanostore?)
	return (
		<main className="container">
			<H1>Dashboard</H1>
		</main>
	);
};
